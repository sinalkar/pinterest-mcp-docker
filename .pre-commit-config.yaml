repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.2
    hooks:
      # lint — mirrors: ruff check .
      - id: ruff
        args: [--fix]
      # format — mirrors: ruff format --check .
      - id: ruff-format
